<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorPrimary"
    android:padding="16dp"
    tools:context=".SignUp">

    <androidx.appcompat.widget.Toolbar
        app:navigationIcon="?attr/homeAsUpIndicator"
        android:id="@+id/toolbar_signUp"
        android:background="?attr/colorPrimary"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_width="match_parent"
        android:layout_height="50dp" >

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Formulário"
            android:textColor="@color/white"
            android:layout_gravity="center_horizontal"/>

    </androidx.appcompat.widget.Toolbar>

    <EditText
        app:layout_constraintTop_toBottomOf="@+id/toolbar_signUp"
        android:id="@+id/edtNomeSignUp"
        android:hint="Nome"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/white"
        android:padding="12dp"/>

    <EditText
        android:id="@+id/edtEmailSignUp"
        android:hint="Email"
        android:layout_marginTop="16dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/white"
        android:padding="12dp"
        app:layout_constraintTop_toBottomOf="@+id/edtNomeSignUp"/>

    <EditText
        android:id="@+id/edtSenhaSignUp"
        android:hint="Senha"
        android:layout_marginTop="16dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/white"
        android:padding="12dp"
        app:layout_constraintTop_toBottomOf="@+id/edtEmailSignUp"/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="Cadastrar"
        android:textColor="?attr/colorPrimary"
        android:backgroundTint="@color/btnLogin"
        app:layout_constraintTop_toBottomOf="@+id/edtSenhaSignUp"
        />

</androidx.constraintlayout.widget.ConstraintLayout>